<template>
	<div class="txtToAudio">
		<!-- <audio :url="url" controls />
		<a-input placeholder="Basic usage" v-model="name" />
		<a-input-search
			placeholder="input search text"
			size="large"
			@search="transformation"
			v-model="inp"
		>
			<a-button slot="enterButton">
				Custom
			</a-button>
		</a-input-search> -->
		11111
	</div>
</template>

<script>
// import { getAudioURL } from 'api/api';
export default {
	// data: () => ({
	// 	url: '',
	// 	inp: '',
	// 	name: ''
	// }),
	// created() {},
	// methods: {
	// 	transformation() {
	// 		getAudioURL({ txt: this.inp, name: this.name }).then((res) => {
	// 			let successCode = res.data.success;
	// 			if (successCode) {
	// 				let data = res.data;
	// 				window.open(data.url);
	// 				console.log(data);
	// 			} else if (res.data.error.code === '012') {
	// 				window.open(res.data.error.url);
	// 			}
	// 		});
	// 	}
	// }
};
</script>

<style lang="less" scoped></style>
